#include <stdio.h>

int main() {

    int n, i;

    scanf("%d", &n);  // 有n组数据

    for (i = 0; i < n; i++) {

        int a, b, c, d;

 

        // 读入4个整数，对这4个整数排序，并输出结果

        // 请将排序代码写在这里，不能使用循环语句
        
        scanf("%d %d %d %d",&a,&b,&c,&d);
        
        int arr[4];
		       
		arr[0]=a;
		arr[1]=b;
		arr[2]=c;
		arr[3]=d;
		
		int current = arr[3];
		int index = 3;
		
		if(arr[index] < arr[0]){
		    index = 0;
	    }
		if(arr[index] < arr[1]){
			index = 1;
		}
		if(arr[index] < arr[2]){
			index = 2;
		}
		arr[3] = arr[index];
		arr[index] = current; 
		
		current = arr[2];
		index=2;
		
		if(arr[index] < arr[0]){
		    index = 0;
	    }
		if(arr[index] < arr[1]){
			index = 1;
		}

		arr[2] = arr[index];
		arr[index] = current;

		current = arr[1];
		index = 1;
		
		if(arr[index] < arr[0]){
		    index = 0;
	    }

		arr[1] = arr[index];
		arr[index] = current;
		
		printf("%d %d %d %d\n",arr[0],arr[1],arr[2],arr[3]);	
	}//for 别看错了 // 

   

    return 0;

}
